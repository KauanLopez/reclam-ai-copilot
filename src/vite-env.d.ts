/// <reference types="vite/client" />

// Declara o objeto global 'firebase' para que o TypeScript
// o reconheça em toda a aplicação, já que ele é carregado
// pelos scripts no arquivo index.html.
declare var firebase: any;
